@model IEnumerable<Technology>

<table class="table">
    <thead>
        <tr>
            <th scope="col">Название</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var technology in Model)
        {
            <tr>
                <td> @technology.Title </td>
                <td class="float-right">
                    <a asp-action="Update" asp-route-id="@technology.Id" class="btn btn-outline-primary">Редактировать</a>
                    <a asp-action="Remove" asp-route-id="@technology.Id" class="btn btn-outline-danger">Удалить</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<p>
    <a asp-action="Create" class="btn btn-primary">Добавить</a>
</p>