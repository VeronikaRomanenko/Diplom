@model Discipline

<div class="my-form">
    <form asp-action="Create" class="form-horizontal text-center">
        <span class="heading">Добавление дисциплины</span>
        <div>
            <div class="form-group">
                <input class="form-control" type="text" asp-for="Title" placeholder="Название" />
                <span asp-for="Title" />
            </div>
            <div class="form-group">
                @foreach (Technology t in ViewBag.Technologies)
                {
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" value="@t.Id" id="defaultCheck_@t.Id">
                        <label class="form-check-label" for="defaultCheck_@t.Id">
                            @t.Title
                        </label>
                    </div>
                }
            </div>
            <div class="form-group">
                <a asp-action="Index" class="btn btn-primary">Отмена</a>
                <button type="submit" class="btn btn-primary">Добавить</button>
            </div>
        </div>
    </form>
</div>